import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  testPlaque: {
    fontSize: 17,
    color: 'rgba(96,100,109, 1)',
    lineHeight: 24,
    textAlign: 'center',
  },
});
